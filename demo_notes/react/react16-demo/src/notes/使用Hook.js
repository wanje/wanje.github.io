import React, { useState } from "react";  //? 这里的 React 对象导入不能省略？

//! Hook 是 React 16.8+ 的新增特性，可以让在不编写 class 的情况下使用 state 以及其他的 React 特性。

//* useState
function useStateHook() {

}

export default function() {
  return (
    <div></div>
  )
}